# tests/ — ожидает наполнения

schema_tests / reference_calculations / golden_cases / dependency_checks /
traceability_checks (§47).

**Статус: пусто (по замыслу этой фазы).** Режим работы — документация, минимум кода;
исполняемые проверки — Tier 5 (`34_Test_Strategy`). Формы объектов уже описывают,
ЧТО эти проверки должны проверять (discriminating-pair, mutation, fail-closed,
provenance), но сам валидатор здесь ещё не реализован. См. `46`.
