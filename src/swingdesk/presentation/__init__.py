"""CLI, reports, web API and notification surfaces.

Owns no logic. Every surface calls the same application services; a rule that exists here and not
in the CLI is a defect (PRODUCT_SURFACES 1).
"""
