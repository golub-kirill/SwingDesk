# Pre-registrations

One file per study, `PR-NNN-<slug>.md`, written and committed **before** the study runs. The template
and the rules are in `../05-validation/PREREG_TEMPLATE.md`.

A registered study that has not run is the normal state. Registration is cheap; it is the thing that
has to happen first, not the thing that has to happen last.

## Index

| ID | Question | Status | Blocked on |
|---|---|---|---|
| `PR-001` | Does the trend definition change which population is selected, or only its size? | **reported — REJECT** | — |
| `PR-001b` | Does definition E's ADX threshold change the answer, across its whole range? | not written | — |
| `PR-005` | Do the trend definitions' populations behave differently, net of costs? | **reported — REJECT** | — |
| `PR-002` | Does a regime classifier improve decisions, or only partition them? | **reported — INCONCLUSIVE**, corrected 2026-08-16 (was `ACCEPT`) | — |
| `PR-003` | Is √252 annualisation wrong enough to matter for this return series? | not written | a daily return series |
| `PR-004` | Do the process-score weights change any ranking? | not written | ~100 journalled trades |
| `PR-006` | Does measured live slippage match the modelled figure? | not written | a forward test — id reserved by `DR-004`, 2026-08-02 |
| `PR-007` | Does the base strategy have positive expectancy net of **measured** costs? | **registered** | — (re-fetch done 2026-08-13, all 68 instruments; see its own §10) |
| `PR-008` | Is the assumed 5bp slippage an understatement of the spread this universe pays? | **reported — INCONCLUSIVE**, then corrected | — |
| `PR-009` | Is a −15R drawdown limit distinguishable from ordinary sequence luck? | **registered**, and its subject moved — see its §10 first amendment, 2026-08-25: the live threshold is 20 percent of equity and has never been −15R | ~~a trade log — none exists~~ **it exists** (`results/PR-005-trades.csv`, 2026-08-16). Blocked instead on that log no longer reproducing (`TODO.md` §5) and on the research suspension |
| `PR-010` | Does EDGE resolve the spread level Corwin-Schultz and Abdi-Ranaldo could not? | **reported — REJECT** | — |
| `PR-011` | Does a 2 × ATR stop behave as the risk model assumes, on names whose ATR is a large fraction of their price? | **reported — `reject`** 2026-09-04, and the direction is the REVERSE of the one predicted: overshoot falls monotonically as volatility rises, −0.0747R between the extreme bands with an interval excluding zero. The sample rule was MET | — |
| `PR-011b` | Should whole instrument CLASSES whose stops are unenforceable — bond and foreign-market ETFs — be screened out? | not written | — **exploratory in advance** when written: the class evidence in `TODO.md` §5 includes a sign flip seen on the fitted data, so no class study judged on expectancy can be confirmatory. `PR-011`'s appendix carries the constraint |
| `PR-012` | Does a cross-sectional ranking beat plain momentum on a capacity-constrained book? | **reported — REFUSED** 2026-08-24, the minimum sample is not met on two of three arms | `CARD-001`; the capacity cap caps the sample, and both caps are ratified |
| `PR-013` | Does relative strength separate forward returns **at all**, measured on names rather than on a four-position book? | **reported — `inconclusive`** 2026-08-24, and the verdict understates it: **all six GROSS intervals include zero**, so the ordering does not separate forward returns before costs are considered at all. **Exploratory by declaration** (§0b) | The sample rule was MET — 142 holdout dates against a minimum of 100 — which is what `PR-012` could not reach. `CARD-001`'s inputs stay `unset` |
| `PR-014` | At what holding period, if any, does the ratified selection rule earn a net excess that excludes zero - and is the shortest such period longer than the twenty sessions `exit.max_holding_period` now assumes? | **reported 2026-09-06 as `ACCEPT` at 126 sessions, then CORRECTED to `inconclusive` and downgraded to EXPLORATORY on 2026-09-07** — amendment A-3, after the data was seen (`PREREG_TEMPLATE` rule 3). It charged GROSS turnover: `252 / horizon` full book turns a year, where a book that re-picks a name holds it. Measured net turnover is 39.0% per rebalance at twenty sessions against the 12.6 full turns charged, so the overcharge was 2.69× at the short end and 1.02× at the long end — a monotone function of the only variable the study varied. Corrected, no horizon replicates on the holdout. Each of the two changes since publication (the cost model, the 10y backfill) is independently sufficient to withdraw the `ACCEPT`; `tools/attribute_pr014_flip.py` has the 2×2 | **The horizon itself has never been tested**: `exit.max_holding_period` is 20, `assumed:DR-012`. Every prior measurement of this family fixed a holding period and asked about the signal, and every one used NON-OVERLAPPING windows - which is why 126 sessions yields seventeen observations and nothing can be resolved there. This registers the standard overlapping-portfolio construction and a moving-block bootstrap to match, plus a primary/holdout split whose whole job is to stop the sweep picking its own maximum. 12 trials |
| `PR-015` | Held for twenty sessions in a FOUR-POSITION long-only book, does ANY of five selection signals earn a net excess over `SPY` whose interval excludes zero and replicates out of sample? | **reported — `INCONCLUSIVE`** 2026-09-07. The only arm whose interval excludes zero is `LOWVOL_126` and it LOSES — 18.22% a year on the primary window, replicated on all four cells — so §6's both-negative branch fires. **The finding is not the verdict, and validation corrected it the same day**: nothing separates from zero. Given an interval of its own the eligible decile does not either — `MOM_252_21` reads **+9.75% [−5.46%, +24.76%]** gross at ~130 names, before any cost, and contains zero on both holdouts. The four-position book IS 1.4× to 3.9× noisier, measured — a real constraint on the instrument and not the reason nothing works | **The first study here to change the SIGNAL.** `PR-013`, `PR-014` and every exploratory sweep hold one family — relative strength — and vary horizon, construction, universe or cost. `PR-014` removed the horizon as an explanation: the ratified rule earns nothing that replicates at any hold from one month to a year. Five arms, one the incumbent as a control, one REVERSAL — the sign the literature documents inside this holding window and which nothing here has ever tested. **Amendment A-2, owner instruction:** the book is `risk.max_concurrent_positions` = 4, entered 5 sessions apart and overlapping four deep, because the decile picks who is ELIGIBLE and the ratified cap picks who is TAKEN. Two holdouts at once, time AND names. A POWER FLOOR is registered with it — `PR-012` refused a verdict on a four-position book, and a grid where nothing was measurable is not a refutation. 5 trials |
| `PR-016` | Under the RATIFIED exit — stop `2.0 × ATR(14)`, target **1R**, time 20 sessions — does the ratified screen change the DISTRIBUTION of trade outcomes: win rate, mean net R, and the shape of the tails? | **reported — `ACCEPT`, and PRELIMINARY** 2026-09-08. The paired difference excludes zero on BOTH windows — **+0.096R** [+0.057, +0.137] in sample, **+0.088R** [+0.018, +0.158] out — and survives triple costs, growing to +0.311R and +0.231R. **The first affirmative verdict this project has produced, and both arms lose money**: the ranked arm −0.100R a trade against a break-even win rate of 54.22%, the control −0.192R. §6 reads the DIFFERENCE and never asks whether either arm is profitable, so what it licenses is that the screen LOSES LESS. **The finding that is not the verdict**: the ratified 1R target caps every winner and no loser, so the distribution is left-skewed — **skew −2.41** where `PR-005` without a target reads +1.61 — and the screen's largest measurable effect is that left tail, p1 −1.735R against −2.863R and a worst trade of −7.16R against −47.24R, while p95 is IDENTICAL at +0.971R, the target minus costs. 8.91 years, not the ten instructed: the store holds ten years of bars and a study needs its lookback first | **The first study here that can run the ratified exit at all.** `ExitPolicy` implemented two of the course's four slots and said so; `exit.target_r_multiple` was ruled 1R on 2026-09-01 (`DR-029`) and lived only in `broker/submit.py`, so every backtested trade ran the stop and the clock while every live trade also carried a take-profit leg. `DR-042` closes that. Distinct from `measure_exit_surface`, which sweeps a GRID on unselected entries, and from `PR-015`, whose unit is a book's periodic return rather than a trade. **The unit is one trade and nothing here licenses a statement about portfolio return.** Two arms — every liquid name as the control, the ratified decile as the hypothesis — and §6 reads only the PAIRED difference. IIS/OOS at `PR-014`'s own boundary, so the cut was not chosen after seeing these data. A window narrower than **200 sessions** is refused and every non-default window is logged, because a reslicing nobody counts is data snooping with a flag. 2 trials |
| `PR-017` | Against a target that already caps every winner, does taking HALF at 1R and carrying the rest beat closing it all — and does either beat no target? | **reported — a POWERED null**, and `INCONCLUSIVE` only because §6 has no branch for one. 2026-09-08. **+0.0015R** [−0.0286, +0.0378] in sample, **+0.0247R** [−0.0162, +0.0780] out — both INSIDE the 0.15R floor at half-widths of 0.033R and 0.047R, against a registered MDE of 0.05R. **Selling half at 1R earns the same as closing it all there.** What it changes is the TAIL, for nothing: p95 **+0.970R → +1.625R**, two thirds of the way to the no-target arm's +2.407R, with the mean unmoved — against a capped incumbent a partial caps LESS. And `M54-T0830`'s mathematical disadvantage is real for a reason the course does not give: at 3× costs the partial is worse by **−0.0356R** [−0.0627, −0.0043], because it is a **third fill** and pays slippage for it. A transaction cost, not a forgone tail. §9's reproduction check passed to every digit out of sample | **Every exit this project has ever measured is all-or-nothing.** `measure_exit_surface`'s 25 cells sweep two prices and close the whole position at whichever comes first; `PR-016` runs the ratified pair. No study, no measurement and no tool had ever sold PART of a position, and until 2026-09-08 `ExitPolicy` could not express one. **The course has an opinion this study exists to test**: `M54-T0830` asserts the partial carries `Математические недостатки` — but that argument is against letting a winner run, and this system does not let winners run. `PR-016` measures p95 at **+0.971R in both arms**, which is the ratified target minus costs. Against a capped incumbent a partial caps LESS. Three exits on ONE entry set, so every difference is the exit and the pairing is exact — which is why the power floor tightens to 0.15R and the minimum detectable effect to 0.05R, both taken from `PR-016`'s own realised dispersion rather than from `PR-005`'s. Sets no parameter: `exit.partial_trigger`, `exit.partial_fraction` and `exit.stop_move_after_partial` are the owner's. 2 trials |
| `PR-018` | On the entries the ratified screen actually SELECTS, does the ratified exit policy earn more than holding to the clock — and what does it buy on the risk side if it does not? | **reported — `INCONCLUSIVE`, and it is not a null**, PRELIMINARY. 2026-09-08. Both difference intervals are WIDER than §8's 0.15R floor — 0.451 in sample, 0.268 out — so §6 refuses to read either window, including the out-of-sample one whose interval excludes zero. **§3's MDE of 0.05R was wrong.** The arms do not share entries — **7,305 hold-only against 11,019 ratified** — ~~and that is why~~: **corrected 2026-09-12**, paired exactly the same kind of contrast still reads a half-width near 0.19R (`PR-019-power.json`), so the width is mostly the stop-versus-no-stop difference moving month to month, not the entry gap. **What it MEASURED**: `hold_only` earns **+0.0645R** and **+0.0404R** where the ratified exit loses 0.0797R and 0.1144R — the first positive mean net R on selected entries this project has produced — and pays for it with a tail in which **21.7% of trades run below −2R** against the ratified policy's **2.3%**, 7.9% below −3R against 0.4%, worst excursion **−23.81R against −7.74R**. **The stop does that work and the target does not**: `ratified_no_target` reads −0.0261R / −0.0650R at 3.0% below −2R. **And selection is not separable from the exit** — `hold_only` is +0.0404R on the selected decile against **−0.1174R on every admitted name** out of sample, a **+0.158R** screen effect larger than `PR-016`'s +0.088R. §9's reproduction check matched `PR-016` to every digit on both windows | **The comparison has never been made on entries anyone would trade.** `measure_exit_surface` makes it on UNSELECTED names and is exploratory; `PR-016` and `PR-017` compare exits to each other and never to none. On those unselected entries, with the SAME one round trip and therefore the same cost, buy-and-hold reads **+0.140R gross** against the ratified cell's **+0.042R** — the 1R target costing about 0.04R and the 2 ATR stop about 0.056R more. **If that survives on selected entries it is the largest effect this project has measured.** §3 predicts H1 FAILS, and the hypothesis is registered that way round on purpose: the incumbent is what costs something, so the incumbent proves itself. **§6 reads the MAE beside the mean, in the same paragraph** — an exit that costs 0.1R and removes a −47R tail is a different object from one that costs 0.1R and removes nothing, and a study reporting only the mean would answer half the question while sounding like it had answered all of it. **First study to carry `PREREG_TEMPLATE` rule 10's `NULL` branch**, which `PR-016` and `PR-017` earned and could not have. 2 trials |
| `PR-019` | On the entries the ratified screen selects, what holding period and stop width does the combination want - and does the best of them, chosen on the older market, survive the current one? | **reported — `INCONCLUSIVE`, exactly as §3 predicted before the data**, PRELIMINARY. 2026-09-12. §6 selected **`h60_stop4.0`** in sample; out of sample it beats the ratified exit by **+0.1888R [+0.0551, +0.3128]** with a thinner tail — **0.81% below −2R against 2.56%, worst −4.67R against −7.25R** — but the interval is 0.258 wide against the 0.15R floor and its own mean, **+0.0528R [−0.1106, +0.2234]**, contains zero. Predicted half-width 0.137, realised 0.129. **Across the grid a 4 ATR stop beats the ratified exit at every hold**, and a 2 ATR stop held longer FATTENS the tail — the hold and the stop are one decision. Unselected, the same exit reads +0.0202R out of sample: the improvement is the exit, and with no market null it may partly be drift. §9 reproduced `PR-016` and `PR-018` to every digit | **Owed by `STRATEGY_CONTRACT` C-7 and the owner's point that raised it: the hold must be researched and per strategy.** `exit.max_holding_period` is 20, `assumed:DR-012`, and no study has varied the per-trade hold on selected entries. Twelve cells - holds 10/20/40/60 x stops 2.0 ATR/4.0 ATR/none, no target - on ONE entry set spaced at 60 sessions so the pairing is exact. **The split buys selection**: one cell chosen in sample by a fixed rule, read only out of sample, which asks the owner's 2026-09-08 question directly - does the old market's best survive the current one. **First study sized by its own variance estimate before it registered** (`tools/power_pr019.py`), and the estimate predicts the answer's shape: precision falls with the hold, so a 40- or 60-session cell cannot be confirmed at the 0.15R floor, and `INCONCLUSIVE` there is written down as "cannot tell" before the data. 12 trials |
| `PR-019b` | Does `PR-019`'s candidate exit earn more than the index over exactly the same days — or is it the market's drift? | **reported — `INCONCLUSIVE`, for precision and not for sign**, PRELIMINARY. 2026-09-13. Out of sample the same notional in `SPY` over each trade's own sessions earned **+0.2071R** against the candidate's +0.0528R: a difference of **−0.1543R [−0.2930, −0.0501]**, wholly below zero and 0.243 wide against the 0.15R floor, so §6 refuses it. Every other reading but the index-charged-costs one also excludes zero — in sample −0.1057R, at 3× trade costs −0.2512R, and on every admitted name without the screen **−0.2772R** — and three known biases favoured the candidate throughout. **The power estimate was wrong**: 0.0743 predicted, 0.1214 realised, 1.69× in sample too; it treated entry months as independent where overlapping 60-session holds make the index legs autocorrelated. §9 reproduced `PR-019` to every digit | **Owed by `STRATEGY_CONTRACT` C-3**: `PR-019`'s selected `h60_stop4.0` reads +0.0528R out of sample and the same exit on every admitted name +0.0202R, which is what drift would look like — and no study has ever compared a TRADE under a stop with the index over that trade's own sessions. Each trade is paired with the same notional in `SPY` over its own days, in its own R, and the power estimate (`tools/power_pr019b.py`) measured what that buys before registering: a predicted half-width of **0.074R** where the trade alone reads 0.181R — readable at the 0.15R floor by 0.0007. **Predicted to FAIL** (`NULL` or `REJECT`), and three known biases — survivorship, beta above one, the missing dividend — all point the other way, so an `ACCEPT` is the weakest outcome it can produce. The holdout is not fresh: `PR-019` read the trade leg's level there, so `ACCEPT` cannot mean profitable, and §0 says so first. 1 trial |
| `PR-020` | With the exit's cost out of the way, does the ratified screen know anything — the selected decile HELD, no stop, against its own universe over the same days? | **reported — `INCONCLUSIVE`, and §9 refuses even that** 2026-09-13. Out of sample the difference is **−0.1412R [−0.4815, +0.0360]**, 3.7× wider than the 0.0704 promised, and the null's own check read −0.0669R against the 0.05R §9 allows. **The cause, measured after the run: the pool leg is in the trade's R**, and on 2022-11-04 the screen's top decile held T-bill ETFs whose `entry / risk` exceeds 1,000 — 38 trades carry 85% of the out-of-sample difference, the other 7,198 average −0.021R. In sample the estimator held, 0.0558 against 0.0732. §9 reproduced `PR-019` to every digit | **Owed by `measure_universe_null` (`EVIDENCE_SUMMARY` §20.7)**, which found the exit the largest cost of `PR-019b`'s loss and the screen's own contribution not distinguishable from zero. **Sized before it registered** (`tools/power_pr020.py`, overlap-aware, dispersion only): predicted out-of-sample half-widths of trade − pool **0.0704 at 5 sessions**, 0.0781 at 10, 0.1005 at 20, 0.1092 at 40, 0.1828 at 60, against the 0.075 the floor needs. Precision falls with the hold, so the only readable hold is the shortest, and the hold was chosen by that alone; the owner ruled on 2026-09-13 that the question be answered rather than shelved. **The pool pays the trade's slippage**, so the difference is selection and not the round trip, and the zero-cost pool is a perturbation. No stop and no target, so `DR-042` does not touch it. §9 reproduces `PR-019`'s binding cell and checks the null against itself. Predicted `NULL`. 1 trial |
| `PR-021` | Bought as last week's biggest losers and held one to five sessions, do US stocks beat `SPY` after costs? | **reported — `REJECT`** 2026-09-13. Half A selected five sessions; on half B the book trails `SPY` by **−24.95 points a year [−38.82, −10.09]** net of `DR-005`, and every other hold is further below. **Before costs there is no edge either**: −3.67 against `SPY`, **+1.59 [−8.29, +11.89] against its own universe**, and the break-even cost against `SPY` is negative at every hold on both halves — so no execution time rescues it. Beta 1.45, worst drawdown −41.4% against `SPY`'s −20.0%. §9's four checks held; width 28.73 against 32.78 predicted |
| `PR-022` | Held while above its ten-month average and in T-bills otherwise, does `SPY` earn more per unit of risk than holding `SPY`? | **reported — `INCONCLUSIVE`** 2026-09-14, as §3 predicted. Sharpe **0.625 against 0.615**, a difference of **+0.011 [−0.370, +0.389]**. Beside it: worst drawdown **−26.9% against −55.2%**, volatility 12.6% against 19.7%, compound return **7.34% against 10.70%** a year — a risk dial, not an edge. In 2008 the rule lost 4.7% through the index's −55.2% fall; in the sharp falls since it re-entered late, and over the last 48 months it earned 9.67% against 19.81%. Exploratory: a fixed 64% `SPY` / 36% `BIL` at the same volatility earned 7.73% at Sharpe 0.655, worse only in 2008. §9 reproduced the power estimate to the digit |
| `PR-023` | Five asset classes, each held above its ten-month average and in T-bills below — more per unit of risk than `SPY`? | **reported — `INCONCLUSIVE`** 2026-09-14, as §3 predicted. `TIMED_5`'s excess Sharpe **0.380 against `SPY`'s 0.543**, a difference of **−0.163 [−0.566, +0.190]**. Beside it: the timed five **never fell more than 13.1% in nineteen years** — +2.5% through 2008 — and earned **4.01% a year against 10.70%**. `HOLD_5` reads **`NULL`**, −0.169 [−0.345, +0.007]: the five held did not beat `SPY` per unit of risk, because the other four earned 2.5% to 5.2% a year to its 10.7%. §9 reproduced both widths to the digit | **Chosen by the agent under the owner's "продолжай"** (`AGENTS.md` §14), owed by `PR-022`'s not-run list. Faber's (2007) headline strategy: a fifth each in `SPY`, `EFA`, `IEF`, `VNQ`, `DBC`, each fifth in `BIL` at or below its own ten-month average, rebalanced at each month's first open, `DR-005` on every unit traded. **Two arms, both counted**: `TIMED_5` carries the verdict, `HOLD_5` — the same five never timed — says what diversification does alone. **The statistic is the Sharpe of the excess over `BIL`**, declared against `stats.sharpe_convention`'s rf = 0 because rf = 0 pays a cash-holding book for the T-bill yield — `PR-022`'s own difference turns from +0.011 to −0.027 without it. **Sized first** (`tools/power_pr023.py`): `TIMED_5` 60.2% in risk assets, width 0.755 — sign only; `HOLD_5` width 0.353 — `NULL` reachable. Predicted `INCONCLUSIVE` for the rule, `REJECT` or `NULL` for the mix. §0 says the window's history is known and biases against both. 2 trials | **The owner's pick after `PR-021`**: the index with drawdown control. Faber's (2007) rule as published — month-end close against the mean of ten month-end closes, `SPY` above and `BIL` at or below, switched at the next open — against holding `SPY`, both on TOTAL return, `DR-005` at 25 bps a fill (about 25× `SPY`'s spread, so it leans against the rule). 2007-06-01 to 2026-09-11, where both legs exist and the rule's design ends, with a `window rationale:` — drawdowns are the subject and 48 months hold one. **The first study here without survivorship bias.** Data by the new `tools/fetch_history.py`. **Sized first** (`tools/power_pr022.py`, widths only): invested 78.0%, 1.66 switches a year, 0.83 points a year of cost; the Sharpe difference's interval 0.759 wide against the 0.40 floor, so `NULL` is unreachable and the sign reads only above ±0.38. **The most public study in the programme** — the window's history is known to the author, and §0 says so. Predicted `INCONCLUSIVE`; the drawdown path is reported beside it and never read. 1 trial | **The first study in a new CLASS**, on the owner's ruling of 2026-09-13 after the relative-strength family was measured out at selection, exit and sizing (`EVIDENCE_SUMMARY` §20.9). Every study before it bought winners; `PR-015`'s `REVERSAL_21` formed on 21 sessions and held 20 in a four-name book. **Per dollar, never in R** — `PR-020`'s lesson. Common stocks only, by the directory's `is_etf` flag, because a leveraged fund sits in the loser tail by construction. The live liquidity rule, lag 3. The bottom decile by five-session return, bought at the next open, held H in 1..5 as an overlapping book, charged `DR-005` on measured net turnover, read against `SPY` over the same open-to-open sessions. **Names split**: half A selects the hold, half B — never loaded by the selection or the power estimate — is judged. **Sized first** (`tools/power_pr021.py`, half A only): turnover 38.0% of the book a session at one session down to 16.8% at five, which `DR-005` charges 47.9 to 21.2 points a year; every interval against `SPY` ~33 points wide against the 20-point floor, so `NULL` is unreachable and the study can read a large effect's SIGN only — the floor gates `NULL` alone, with `PR-019b`'s refused reading as the reason. Predicted `REJECT` or `INCONCLUSIVE` on cost arithmetic; the break-even cost a side is printed beside `DR-040`'s curve and never read. 5 trials |
| `PR-024` | Does `CARD-001` earn more per dollar entering five minutes before the close than at the open? | **reported — `ACCEPT`** 2026-09-17, against §3's prediction. Entering at 15:55 instead of the open earns **+0.311% per dollar a trade [+0.228, +0.418]** on 8,878 of 9,820 drawn entries, net of each name's own SIP spread; cost-adverse **+0.200%**, date-weighted +0.325%, 11:00 +0.313%. **All of it the spread**: the card's names pay 37.8 bps a side at the open and 5.0 at 15:55, and the gross part is −0.016% [−0.105, +0.090]. **It does not make the card profitable** — at 15:55 it earns +0.024% a trade [−0.396, +0.456]. Not measured: the opening auction, which an order can join and pay no spread. §9 held: parity 0 of 9,820, width 0.69 of predicted. Exploratory: the registered rule dropped entries for failures of the unread 11:00 arm and of a perturbation; without that, +0.370% | **The owner's first study after `CHARTER` A-003 reopened research** ("Да, начать с этого"), owed by `DR-040` §9.4. §9 priced the same entry at 09:30, 11:00 and 15:30 and read +0.310% near the close, but charged every entry the difference of two MEDIAN spreads of the whole universe, sampled the universe rather than the card's names, and flipped to +0.049% when dates were weighted equally. This changes exactly those three: `CARD-001`'s own selection, **each entry's own SIP quoted spread at the moment it traded** (`tools/fetch_entry_quotes.py`, `market_data.quotes`), and the same number of names on every date. **Three arms on one entry** - `O` at the 09:30 minute, `C` at 15:55 (the verdict), `T` at 11:00 (counted, never read) - walked as the engine walks them except that the entry session is read only from the minutes after each arm's fill; `--parity` proves the walk is `run_arm`. Per dollar, month-clustered, last 48 months. **Sized first** (`tools/power_pr024.py`, daily-bar proxies, widths only), and the sample was drawn and committed before any minute or quote was fetched. Predicted `NULL` or `INCONCLUSIVE`. 2 trials |
| `PR-025` | Does an entry that joins the opening auction earn what the 15:55 entry earns? | **reported — `INCONCLUSIVE`, EXPLORATORY** 2026-09-19. The registered run's `REJECT` (−3.9 points a trade) was a unit error: the SIP tape's crosses are as traded, the bars carry later splits and spin-offs — 620 sessions off by 2×, 4×, 10×… (amendment A-1, made after the data, so exploratory). Corrected: **the opening auction less 15:55, +0.007% [−0.089, +0.084]** — the `NULL` §3 predicted — and against the continuous open **+0.375% [+0.316, +0.435]**. `PR-024`'s saving is a routing choice (`opg`) | **The owner's pick after `PR-024`** ("Сначала аукцион"). `PR-024` priced the open as an order meeting the continuous book after the cross and found 15:55 better by +0.311% a trade, all of it the spread; an order IN the opening auction pays the cross's single price and no spread, and moving an order there is a routing change (`opg`) where 15:55 needs a pass during the session that does not exist. Same draw and stores as `PR-024`; the cross read from the SIP trade tape (`tools/fetch_auction_prints.py`, `market_data.auctions`) as the largest print flagged `O` or `6`. **Four arms**: `A` the opening cross (the verdict, `A − C`), `C` and `O` `PR-024`'s own, `X` the closing cross (counted, never read). Each reading priced on the entries it needs. Sized from `PR-024`'s realised width: MDE 0.14 points. **Predicted `NULL`** — §0 says the arithmetic on `PR-024` already points there. 2 trials |
| `PR-026` | Do `CARD-001`'s picks earn more against `SPY` with leveraged and inverse funds left out? | **reported — `INCONCLUSIVE`, branch `NULL`** 2026-09-19, as predicted: leaving leveraged and inverse funds out moves the card's excess over `SPY` by **−0.007%** a trade [−0.039, +0.018] — 1,508 of 40,240 entries. The card itself trails `SPY` over its own days by −0.27% [−0.65, +0.14]. **None of the four ideas survived the window the exploration never read** (`PR-026-report.md`) | The first of four single changes the owner picked on 2026-09-19 after `PR-024` (card breaks even at 15:55) and asked to keep apart: **one baseline, one draw, one arm per idea, each its own verdict**. The baseline is the card entered at the next session's close; every trade is read in EXCESS of `SPY` over its own days, so `BOTH_NEGATIVE` catches "better, still behind the index". 2018-09..2022-08, 1,006 sessions × 40 names, a window the exploration that suggested the ideas (`entry-features-2026-09-19`) never read. Leveraged/inverse by `run_pr026.levered_name`. Width 0.044 — `NULL` reachable; predicted `NULL`. 1 trial |
| `PR-027` | Do `CARD-001`'s picks earn more against `SPY` bought on a pullback below their 20-day mean? | **reported — `INCONCLUSIVE`** 2026-09-19: the pullback entries less all, **−0.094%** a trade [−0.392, +0.176]. The exploration's best slice, +0.57% on 2022–26, did not come back | The second of the four (`PR-026` carries the shared design). The leaders that just dipped, against the whole decile — a reversal inside a winner, not `PR-015`/`PR-021`'s losers. The exploration's favourite; width 0.522, so only an effect beyond ±0.26 reads. Predicted `INCONCLUSIVE`. 1 trial |
| `PR-028` | Re-priced at the close, does `PR-019`'s wide exit earn more against `SPY` than the ratified exit? | **reported — `INCONCLUSIVE`** 2026-09-19, as predicted: the wide exit less the ratified exit, each against `SPY`, **+0.699%** [−0.992, +2.615] — pointing up, 3.6 wide | The third of the four. `PR-019`'s selected `h60_stop4.0` (4 ATR, no target, 60 sessions) on the same entries, now at the close with measured spreads and against `SPY` over its own days — where `PR-019b` found it short. A re-price of a counted cell on years `PR-019` read, and said so. Width 2.05: only ±1.0 point reads. Predicted `INCONCLUSIVE`. 1 trial |
| `PR-029` | Do `CARD-001`'s picks do differently against `SPY` when `SPY` is below its 200-day mean? | **reported — `INCONCLUSIVE`** 2026-09-19, as predicted: dates below `SPY`'s 200-day mean less above, **+0.477%** [−0.312, +1.317]; 274 dates below | The fourth of the four. Two-sided: the literature expects momentum to suffer after downturns, the exploration saw the opposite on essentially 2022. Width 0.95. Predicted `INCONCLUSIVE`. 1 trial |
| `PR-030` | Which order should carry `CARD-001`'s entries — today's day limit at the signal close, or a limit-on-open (`opg`) at the same price? | **reported — `INCONCLUSIVE`** 2026-09-19, as the power estimate put at near even odds: **`G − D` +0.123%** a drawn entry [−0.075, +0.305], all of it cost (gross −0.023%). Counted and never read: **`J − D` +0.145% [+0.119, +0.174]**, a day limit that also joins the cross. Every order's own net is break-even. §9's split check FAILED as registered (ask vs print; amendment A-1) | **The owner's pick after `PR-025`** ("Сначала измерить"). `PR-025` found the opening auction worth +0.375% a trade over the continuous open — but the live order is a LIMIT: marketable at the open it pays the spread, otherwise it rests and fills at the limit or not at all, and a limit-on-open takes the cross or nothing. **Four orders on `PR-024`'s draw and `PR-025`'s stores**: `D` today's, `G` the limit-on-open (the verdict, `G − D`), `M` the market-on-open (`PR-025`'s `A`), `J` a day limit that joins the cross. A missed order earns 0 and counts. Stated lean: a touch fills the resting order, which favours `D`. Predicted width **0.37** over the 0.30 floor: `NULL` is out of reach, and at the predicted **+0.20** the branch is close to a coin flip between `ACCEPT` and `INCONCLUSIVE` (MDE 0.26). 3 trials |
| `PR-031` | Does the published intraday momentum rule make money on `SPY`, long only — and still, since it was published? | **reported — `ACCEPT`** 2026-09-19, by the thinnest margin: **+0.0336% a day** [+0.0169, +0.0521], **8.5% a year at a Sharpe ratio of 1.01** against holding `SPY`'s 0.83, worst drawdown −9.9%. **Since publication +0.0018% a day [−0.0228, +0.0263], Sharpe 0.06** — the gate cleared by a seventh of a standard error. `QQQ-long` (never read) held up: 8.6% a year since publication, Sharpe 1.06. The first rule here to beat holding `SPY` per unit of risk | **The owner's pick after `PR-030`** ("Внутридневной моментум"), long only by their standing preference. Zarattini, Aziz & Barbon (2024): a noise area from the last 14 sessions' moves from the open, long at HH:00/HH:30 above it and the VWAP, flat at the close, sized to 2% daily volatility, capped at 4×. Here with no look-ahead (decide on the minute that ends at the half hour, fill at the next open), half a cent a share a side, on `SPY` and `QQQ` minutes 2016-01-04..2026-09-18 — a replication's window; the 590 sessions since publication gate `ACCEPT` (`PUBLICATION_FRAGILE`). Predicted width 0.036 points a day (MDE ≈ 6.5% a year): **predicted +0.02% a day**, `ACCEPT` and `NULL` near even. 3 trials |
| `PR-032` | Does the intraday momentum rule still work on funds this project has never read? | **reported — `INCONCLUSIVE`, branch `NULL`** 2026-09-19: the basket since publication **−0.0054% a day** [−0.0293, +0.0199], **+0.0003% gross** — nothing for a cheaper fill to rescue. And the reading §6 never reads: **before** publication the same basket earned +0.0035% a day, 0.9% a year, Sharpe 0.14 — **the rule was never present on these funds**. `SPY` through this tool reproduced `PR-031`'s estimate to 1e-20 | **The owner's pick after `PR-031`** ("Проверить на новых ETF" over writing a paper-trial specification). `PR-031` left two stories fitting one result: the rule decayed when it was published, or `SPY`'s two flat years are one fund's. They differ on funds nobody here has looked at. **`PR-031`'s rule unchanged, long only, on an equally weighted basket of `IWM`, `DIA` and `EFA`** — chosen for what they are, before a minute of them was read — over the 590 sessions since 2024-05-13. Each fund alone and the years before publication are counted and never read. Self-check: `SPY` through this tool's loader must reproduce `PR-031`'s own estimate. Predicted width **0.047 points a day** (MDE ≈ 8.5% a year): predicted **+0.02% a day**, `ACCEPT` and `NULL` near even. 3 trials |
| `PR-033` | Is the money in the night? The overnight return against the session's own, on five index funds | **reported — `INCONCLUSIVE`, branch `COST_FRAGILE`** 2026-09-20: the basket's night **+0.0325% a day** [+0.0045, +0.0602] — **+8.2% a year** — against the session's +3.4%; at a whole cent a share a side the interval touches zero. **It does not beat holding**: Sharpe 0.67 against 0.81. **`IWM` is the exception** (night +13.1% a year at 0.91, session −3.3%, holding 0.56), read after the fact. The last 590 sessions are its best: +12.8% a year at 1.16. The two arms compound back to holding to 3e-16 | **The owner's pick after `PR-032`** ("Ночь против дня"). Published since 2008 and never tested here: an index fund's gain is reported to accrue between the close and the next open. **Two arms on `SPY`, `QQQ`, `IWM`, `DIA` and `EFA`** — `N` bought in the closing auction and sold at the next open, `D` open to close — each paying two sides a day at half a cent a share. **The dividend goes to `N`**, because it detaches at the ex-date's open. The verdict reads the equally weighted basket's `N`; the funds alone, `D`, `N − D` and holding each fund with its dividends are counted and never read. The last 590 sessions gate `ACCEPT` (`RECENT_FRAGILE`). Predicted width 0.056 points a day (MDE ≈ 10% a year): predicted **+0.035% a day**, about +9% a year. 10 trials |
| `PR-034` | Small caps' night, asked properly — on funds `PR-033` never read, at a cent a share, against holding them | **reported — `ACCEPT`** 2026-09-20: the night **+0.0549% a day** [+0.0207, +0.0895] — **13.8% a year at a Sharpe ratio of 1.06**, against holding the same funds' 0.59 — and **the session LOSES 5.4% a year**. All three gates cleared: a whole cent a share (+11.7% a year), the last 590 sessions (+15.7% a year at 1.24), and beating holding. `VB` alone: night 17.3% at 1.36. `MDY`, the mid-cap boundary, is weaker on both sides as the hypothesis predicted. **The first result here to beat holding the asset per unit of risk** | **The owner's pick after `PR-033`** ("Да, проверяем"). `PR-033`'s one lead was `IWM`'s night — Sharpe 0.91 against holding's 0.56 — read after the fact, one of ten arms. This asks it as a hypothesis on `IJR` and `VB`, with `MDY` as the mid-cap boundary case, counted and never read. **The two gates `PR-033` failed are written into the decision rule**: a whole cent a share a side, and beating the same funds held outright per unit of risk (`NOT_BETTER_HELD`). Stated in §0: these funds hold companies overlapping `IWM`'s, so this tests the wrapper and the gates, not an independent sample. The session rule differs from `PR-033`'s and says why before the run. Predicted width 0.069 a day (MDE ≈ 12.4% a year): predicted **+0.048% a day**. 6 trials |
| `PR-035` | The same dollar twice: small caps overnight, `SPY` through the session, against holding `SPY` | **reported — `INCONCLUSIVE`, branch `COST_FRAGILE`** 2026-09-20, the branch §3 rated likeliest: the book less holding `SPY` **+3.25% a year** [+0.0002, +0.0272 a session] — clearing zero by a rounding error — and **+0.35% at a whole cent a share**, which does not. **The book itself returned 18.9% a year at a Sharpe ratio of 1.01** against the index's 15.7% at 0.89. Gross the margin is 6.1%: the edge and the trading cost are the same size. `CARD-002` stays one leg | **The owner's pick after `PR-034`**: shown the night earns 13.8% against holding `SPY`'s 15.7%, they asked for more RETURN and chose the idle day slot over leverage. The book owns `IJR`+`VB` while the market is shut and `SPY` while it is open — **four auction fills a day, the same dollar used twice**. The day legs of book and benchmark cancel, so §0 states the arithmetic before the run: the difference IS small caps' night less `SPY`'s night less the extra trading, about **+4.2% a year**. **`SPY`'s dividends are not earned** by a book that holds it only in the session — about −1.3% a year, the largest term against H1. Predicted width 0.027 a day; **`COST_FRAGILE` is the branch the author rates likeliest** at a whole cent on four sides. 1 trial |
| `PR-036` | Did the night pay BEFORE 2016? The same two arms, on daily bars, back to 2004 | **reported — `INCONCLUSIVE`, branch `COST_FRAGILE`** 2026-09-20 | **The advisory council's sharpest objection**: every reading so far covers 2016-2026, a decade in which the index returned 15.7% a year, so the overnight result could be a wrapper around an epoch. The two prices the arms need are in the DAILY bars, which reach back to 2000 — so `PR-034`'s rule, funds, weighting and costs run unchanged over **2004-01-30 .. 2015-12-31**, a window no tool here has read, holding 2008 and a decade that went nowhere. **The method must prove itself first**: the same reading over 2016-2026 from daily bars must land within 2 points a year of `PR-034`'s minute-based +13.83%, or the older number is not reported as evidence. **Zero trials** — no configuration is evaluated that `PR-034` did not declare. Predicted +11% a year. **Realised +10.94%** at a Sharpe ratio of 0.84 against holding's 0.48, compounding at 10.6% where holding compounded at 8.8% and fell 58.8%; the overlap check landed 0.57 points away, so the price source is sound. `COST_FRAGILE` only because a whole cent a share touches zero in a decade when these funds traded at a third of today's price |
| `PR-037` | Do sectors trend as a block? Industry momentum on eleven sector funds | **reservation released, NOT registered** 2026-09-21 | **The id is released and no trial was spent, because the design was measured before it was registered.** `tools/measure_sector_power.py` replaced the ranking with a coin — so the proxy carries the turnover, the costs, the concentration and the calendar, and no signal — and asked how large an effect this design could separate from zero. Against the funds' own pool: **4.56% a year for a book of two, 3.18% for three, 2.32% for four, 2.16% for five and 2.86% for the tercile rule, against a literature claiming about two.** Only six of eleven gets under it, at 1.67%, and six of eleven is 55% of the pool. **The cross-section binds, not the book size**, so no long-only variant rescues it. `tools/run_pr037.py` is the finished instrument; `RETURN_SOURCE_REGISTER` §3.1 holds the reopening trigger |

**`PR-011`'s question narrowed when it was written, 2026-09-04, and the half that was cut is
`PR-011b`.** The id was reserved on 2026-08-22 for *"should instrument classes that cannot hold a
stop be screened out"*. Writing it exposed that the two halves are not the same study: the ATR-vs-price
half has no prior fitted on it and is confirmatory, while the CLASS half is contaminated — `TODO.md`
§5 records a sign flip in mean net R observed on the fitted data, which `PREREG_TEMPLATE.md` rule 3
makes exploratory for whoever writes it. Splitting them is what keeps the clean half clean; a
citation of `PR-011` written before this date means the wider question.

**Three ids collided on 2026-08-09 and several studies moved.** Three efforts registered studies without
seeing each other. `RECONCILIATION_PLAN.md` D-R4 awards a contested id to the earliest commit
timestamp, so:

- `PR-007` stayed with the base-strategy study (2026-08-08); the effective-spread study became
  `PR-008`. Both ask about costs, from opposite directions.
- `PR-006` stayed **reserved** for live slippage — `DR-004` claimed it on 2026-08-02, before any file
  existed — so the drawdown study became `PR-009`.

That second one is the case this index predicted two paragraphs down and asked someone to fix "if a
third one appears". A third appeared. **A `PR-006` or `PR-007` citation written before 2026-08-09
may mean a different study than the same string written after.** Git history is unedited throughout,
because a registration commit is what proves a hypothesis predated its run.

**`PR-009` is blocked on something the project did not know it lacked.** No reported study here
persisted a trade log, and `BACKTEST_PROTOCOL.md` §3 lists one as the third of the five artefacts the
course requires for a strategy claim. The results are honest; their supporting detail is not
reconstructible. So its step 1 is to reproduce PR-005 under its recorded constants and persist the
log — and if the reproduction does not match the reported aggregates, that mismatch is the result,
reported as `inconclusive` rather than buried.


`PR-003` and `PR-004` are named in `DR-001` and `DR-002` as the studies that would overturn them.
`PR-006` is named in `DR-004` and `PR-007` in `DR-005`, the same way. `PR-005` is required by
PR-001's result: the definitions are not interchangeable, so choosing one needs evidence about what
its population does, not just that it differs. They are listed here unwritten so the debt is visible
rather than implied.

**`PR-006` was reserved on 2026-08-02 and went unlisted here until 2026-08-05.** Reserving an id in
a decision record and not recording it in the index is how the debt stops being visible, which is
the one thing this table exists to prevent. ~~Nothing catches it: `verify_docs.py` fails on a decision
record whose *file* is missing from the decisions index, but an id reserved **by reference only**,
with no file behind it, leaves nothing for a gate to find. Worth fixing if a third one appears.~~

**A third appeared, and gate 29 (`tools/verify_prereg_ids.py`) now catches all three shapes**, 2026-08-24:
a study document missing from this index, an id reserved by reference anywhere in `docs/` and not
listed here, and two **unmerged** branches numbering different studies the same. That last one is
`AGENTS.md` §10.2 as a check rather than a habit — `POSTMORTEM-2026-08-09.md` root cause A is two
efforts whose trees were each internally consistent. Merged branches are excluded deliberately:
this repository's two real collisions — a second `PR-006` and a second `PR-007` — are both on merged
branches, so the numbering was reconciled and the old filenames are correct statements about a commit.

**The spread level is closed by evidence.** Three estimators — Corwin-Schultz (2012),
Abdi-Ranaldo (2017) and EDGE (2024, built to fix both) — cannot resolve it on this universe.
`PR-010` reports a median of 25.65bp per side against its own zero-spread floor of 41.87bp, and
Abdi-Ranaldo's 25.44bp sits under a 33.85bp floor. The two agree to 0.21bp *inside their shared
noise*, which is what common bias looks like. `PR-006` — real fills — is the only route left, and
that is now measured rather than assumed.

Results live in `results/`, one JSON of record plus a written report.

**PR-002 is the first hypothesis this project has failed to refute — on one market.** Breadth
separates breakout outcomes out of sample, under cost stress and under a stricter null than the one
registered — and a survivorship confound could produce the same result with no real effect present.

**Its verdict was corrected from `ACCEPT` to `INCONCLUSIVE` on 2026-08-16.** §6 permitted `accept`
only where the effect held in both countries independently, and the third amendment — written before
any data was seen — had already assigned a single-market result to the inconclusive branch. The
runner implemented the percentile thresholds with no country condition and emitted `accept` anyway.
The measurements are unchanged; the label was wrong. `regime.classifier_rule` moved to
`assumed:PR-002` and **this project now has zero `validated` parameters**. Read
`results/PR-002-report.md` — including its Correction section — before using it.

**The trend-definition family is closed.** PR-001 found the definitions select different
instruments; PR-005 found those different instruments then do the same thing. Three refuted
hypotheses, both pre-registered, and `screen.trend_definition` stays `unset` as a result. See
`results/PR-001-report.md` and `results/PR-005-report.md`.

## Status values

`registered` — committed, not yet run · `running` · `reported` · `abandoned`

An abandoned pre-registration stays in the repository. A study abandoned after seeing partial data is
exactly what vanishes from a dishonest record.
